package com.sungbin.sungbintool.tool


/**
 * Created by SungBin on 2020-10-15.
 */

object CrashReporter {
    /*fun init(context: Context, action: (throwable: Throwable) -> Unit = {}) {
        Thread.setDefaultUncaughtExceptionHandler { _, throwable ->
            ToastUtil.show(
                context,
                "오류가 발생했습니다. (오류 복사댐)\n\n${throwable.localizedMessage}",
                ToastLength.LONG,
                ToastType.ERROR
            )
            Util.copy(
                context,
                "${throwable.localizedMessage} #${throwable.stackTrace[0].lineNumber}"
            )
            action(throwable)
        }
    }*/

    // todo
}